import React from "react";
import { Form, Input, InputGroup, InputGroupAddon, Label, Button } from "reactstrap";

export function CreditCardPayment(props) {
  return (
    <Form>
      
    </Form>
  );
}
